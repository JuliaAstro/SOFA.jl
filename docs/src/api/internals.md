# Internals

Non-exported types and helpers used by the library. These are implementation
details and not part of the public API.

```@autodocs
Modules = [SOFA]
Pages = ["base.jl", "util.jl", "leapseconds.jl"]
Public = false
```
