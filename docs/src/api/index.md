# API Reference

```@docs
SOFA
```

The API mirrors the SOFA C library, organized into the topical pages listed in
the sidebar. Function names match their SOFA C counterparts with the `iau`
prefix removed, so e.g. `iauCal2jd` becomes [`cal2jd`](@ref).

## Index

```@index
```
